import { createEvent } from "effector";

/** Нажимаем кнопку Match orders */
export const clickedMatchOrders = createEvent();

/** Нажимаем кнопку Place order */
export const clickedPlaceOrder = createEvent();
